{
	"name": "goblin-workspace-health",
	"version": "0.1.0",
	"type": "module",
	"scripts": {
		"build": "tsc -p tsconfig.json",
		"check": "eslint . && tsc -p tsconfig.json --noEmit",
		"test": "vitest",
		"lint": "eslint ."
	},
	"dependencies": {}
}
