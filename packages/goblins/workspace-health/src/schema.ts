/**
 * Database schema for workspace-health goblin
 * Currently no database tables are needed for workspace health checking functionality
 */

export async function migrate(): Promise<void> {
	// No database migrations needed for workspace-health
	// This goblin focuses on running health checks and doesn't store persistent data
}
