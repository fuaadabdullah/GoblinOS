apiVersion: v1
kind: ConfigMap
metadata:
  name: overmind-config
data:
  # OpenTelemetry collector endpoint
  otel_endpoint: "http://jaeger-collector:4318/v1/traces"

  # Logging level
  log_level: "info"

  # Node bridge URL (internal service)
  node_bridge_url: "http://overmind-bridge:3030"
