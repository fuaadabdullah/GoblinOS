# Changesets

This folder contains changeset files. Each changeset describes a change and which packages it affects.

To create a new changeset:
```bash
pnpm changeset
```

Changesets will be consumed by the release workflow to version packages and generate changelogs.
